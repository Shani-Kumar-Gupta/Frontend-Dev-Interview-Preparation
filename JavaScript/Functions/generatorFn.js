/*
! What is Iterator Pattern?
  ?: for...of loop ----(used)----> Iterator ----(under the hood to interate)----> Arrays

! Generator Function in JavaScript?
  ?: function* fnName() { }

! YIELD Keyword?
  ?: Used to generate the next value
*/